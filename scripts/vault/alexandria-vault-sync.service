[Unit]
Description=Alexandria vault sync (pull, ingest inbox, re-export, push)

[Service]
Type=oneshot
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=%h/bin/alexandria-vault-sync
